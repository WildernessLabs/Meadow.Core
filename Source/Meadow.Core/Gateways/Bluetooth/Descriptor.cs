using System;
namespace Meadow.Gateways.Bluetooth
{
    /*
    /// <summary>
    /// A descriptor for a GATT characteristic.
    /// </summary>
    public class Descriptor : IDescriptor
    {
        /// <summary>
        /// ID of the descriptor.
        /// </summary>
        public Guid ID { get; }

        /// <summary>
        /// Name of the descriptor.
        /// </summary>
        public string? Name { get; }

        //TODO: do we want to try and pull the name from the known descriptors?
        // maybe if it's null? 
        //public string Name => this.Name ?? KnownDescriptors.Lookup(Id).Name;

        /// <summary>
        /// The value of the descriptor.
        /// </summary>
        public byte[]? Value { get; set; }

        public Descriptor(Guid id, string? name)
        {
            this.ID = id;
            this.Name = name;
        }
    }
    */
}
